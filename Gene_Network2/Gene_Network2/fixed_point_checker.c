/*
B.	Fixed point 검사 모듈 설계
1)	회로의 출력이 fixed point 인지 확인하는 모듈 fixed_point_checker.v 설계
*/
#include <memory.h>



int fixed_point_checker(int time, int X[]) {
	
	memset(state[time], X, sizeof(int) * GENE);


}